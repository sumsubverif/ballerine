{
  "buildCommand": "cd apps/backoffice-v2 && npm install && npm run build",
  "outputDirectory": "apps/backoffice-v2/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
